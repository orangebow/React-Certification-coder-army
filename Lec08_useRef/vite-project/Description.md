Understanding of WHY and HOW useRef works.

✅ What is useRef and why does it exist?
✅ The fundamental problem useRef solves
✅ Two main use cases of useRef
✅ Storing mutable values (interval IDs, timers, counters)
✅ Accessing real DOM elements directly
✅ useRef vs useState - when to use what
✅ Building practical examples from scratch

🔥 Projects We Build:

1️⃣ Stopwatch with Start, Stop, Reset (bug-free implementation)
2️⃣ Auto-focus Search Input
3️⃣ Video Player with Play/Pause/Restart controls
4️⃣ Form handling with useRef
5️⃣ Skip forward/backward functionality

💡 Key Concepts Covered:

    Why useRef doesn't trigger re-renders
    How .current property works
    Preventing memory leaks in intervals
    Direct DOM manipulation in React
    Real-world use cases you'll actually use
